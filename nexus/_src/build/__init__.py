"""The builder: a ``LaunchConfig`` resolved, its assets fetched, and the run assembled around it.

:mod:`.launch` resolves a launch and routes the control kind; :mod:`.assembly` wires the core
components around the physics and the controller the launch picks.
"""
